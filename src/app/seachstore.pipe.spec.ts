import { SeachstorePipe } from './seachstore.pipe';

describe('SeachstorePipe', () => {
  it('create an instance', () => {
    const pipe = new SeachstorePipe();
    expect(pipe).toBeTruthy();
  });
});
